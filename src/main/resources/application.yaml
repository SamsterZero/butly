spring:
  application:
    name: butly
  
  datasource:
    url: jdbc:postgresql://localhost:5432/butly
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}